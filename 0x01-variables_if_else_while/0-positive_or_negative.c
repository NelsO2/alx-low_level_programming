#include <stdlib.h>
#include <time.h>
/* comments enters here */ 

/* main - print if the number is positive, zero, or negative */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* this program prints "progamming is positive, zero, or negative */
	return (0);
}
